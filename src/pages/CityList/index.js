import React, { Component } from 'react'

export default class CityList extends Component {
    render () {
        return (
            <div>
                CityList组件
            </div>
        )
    }
}
